# Bip landing page

Pre-launch landing page for Bip, a tool that turns your real dev work into build-in-public posts.

Single static `index.html`, no build step. Email capture via Formspree.

## Deploy

Static site. On Vercel: import this repo, framework preset "Other", no build command, output directory is the repo root.
